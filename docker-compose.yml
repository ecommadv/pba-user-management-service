version: '3.8'
services:
  postgres_db:
    container_name: 'testdb'
    image: postgres:14.2-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=larlarlar
      - POSTGRES_DB=testdb
    ports:
      - '5432:5432'